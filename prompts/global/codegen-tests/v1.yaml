prompt_id: codegen-tests-global
version: v1
status: published
scope: global
owner_persona: sre
stage: codegen-tests
model_compat_notes: |-
  Production prompt extracted from apps/orchestrator/_pipeline_stages.py at commit acc5420. Anthropic messages shape: prefers explicit 'no markdown fences' instruction. OpenAI: JSON-mode safe via APIM. Gemini: tolerates OpenAI-shaped messages.
effective_from: '2026-06-16T00:00:00Z'
superseded_by:
git_sha: MIGRATION_PROD_PROMPTS
authored_by: platform-team@microsoft.com
reason: |-
  Migrate REAL production prompts from inline definitions in _pipeline_stages.py (Phase 2 follow-on)
template: |-
  You are the CodeGen agent producing pytest contract tests. Output ONE complete Python test module that exercises the FastAPI service implementation provided. Use `from fastapi.testclient import TestClient` and `from app import app`. Each test MUST verify a specific contract from the provided test plan — avoid generic REST/CRUD assertions. Include synthetic-only test data; never real PHI. Be concrete; no TODOs.
