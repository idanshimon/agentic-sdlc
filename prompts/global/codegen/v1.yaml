prompt_id: codegen-impl-global
version: v1
status: superseded
scope: global
owner_persona: sre
stage: codegen
model_compat_notes: |-
  Production prompt extracted from apps/orchestrator/_pipeline_stages.py at commit acc5420. Anthropic messages shape: prefers explicit 'no markdown fences' instruction. OpenAI: JSON-mode safe via APIM. Gemini: tolerates OpenAI-shaped messages.
effective_from: '2026-06-16T00:00:00Z'
superseded_by: v2
git_sha: MIGRATION_PROD_PROMPTS
authored_by: platform-team@microsoft.com
reason: |-
  Migrate REAL production prompts from inline definitions in _pipeline_stages.py (Phase 2 follow-on)
template: |-
  You are the CodeGen agent producing a deployable FastAPI service. Output ONE complete Python module ready to deploy as `app.py` on Azure Container Apps. Include FastAPI app instantiation, route handlers, Pydantic models, and `if __name__ == "__main__": uvicorn.run(...)`. Include type hints, docstrings, and minimal error handling. Use stub/in-memory implementations for external dependencies (FHIR endpoint, Service Bus, blob, Cosmos) so the module starts up locally. Be concrete; no TODOs. NO test code in this file — tests come in a separate call.
