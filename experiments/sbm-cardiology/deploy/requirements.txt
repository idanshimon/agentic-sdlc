# SBM cardiology deterioration alert service — runtime deps only.
# Test deps are in the Dockerfile (kept out of requirements.txt so a pip
# install of just the service doesn't pull pytest).
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
httpx==0.27.2
